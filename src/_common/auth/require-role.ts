// 사용자의 "권한(Role)"을 확인하는 미들웨어입니다.
/** 즉, api 요청자가 특정 권한(Role)을 가지고 있는지 확인하는 미들웨어입니다.
 * 사용자의 역할(예: ADMIN, RESIDENT, SUPER_ADMIN 등)이 API 접근 조건에 맞는지 검사
 * 권한이 없으면 403 Forbidden 에러 반환
 * 권한이 맞으면 다음 미들웨어/핸들러로 진행
 *
 */
